{
  "name": "irl-core-ig-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Basic-auth gate in front of the published FHIR Core Ireland IG",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node --test test/"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
